<UserControl x:Class="DataSphere.Views.Pages.DatabaseGroup.DatabaseViewControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DataSphere.Views.Pages.DatabaseGroup"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:controls="clr-namespace:DataSphere.Controls"
             xmlns:utils="clr-namespace:DataSphere.Utils"
             xmlns:helpers="clr-namespace:DataSphere.Helpers"
             xmlns:dbmodels="clr-namespace:DataSphere.Models.Database"
             xmlns:viewmodel="clr-namespace:DataSphere.ViewModels.Pages"
             mc:Ignorable="d" 
             d:DesignHeight="450"
             d:DesignWidth="800"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="5" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>

        <TreeView
            Grid.Column="0"
            ItemsSource="{Binding ViewModel.AllDatabases, Mode=OneWay}"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch">

            <TreeView.ItemContainerStyle>
                <Style TargetType="TreeViewItem" BasedOn="{StaticResource {x:Type TreeViewItem}}">
                    <EventSetter Event="Expanded" Handler="TreeViewItem_Expanded" />
                </Style>
            </TreeView.ItemContainerStyle>

            <TreeView.Resources>

                <!-- Style gốc có ContextMenu -->
                <Style TargetType="TreeViewItem" BasedOn="{StaticResource {x:Type TreeViewItem}}">
                    <Setter Property="ContextMenu">
                        <Setter.Value>
                            <ContextMenu>
                                <MenuItem Header="Open" />
                                <MenuItem Header="Refresh" />
                                <MenuItem Header="Delete" />
                            </ContextMenu>
                        </Setter.Value>
                    </Setter>
                </Style>

                <!-- Template hiển thị Database -->
                <HierarchicalDataTemplate DataType="{x:Type dbmodels:DatabaseInfo}"
                                ItemsSource="{Binding Tables}">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="🗄️" FontSize="14" Margin="0,0,6,0"/>
                        <TextBlock Text="{Binding Name}" FontWeight="Bold" />
                        <TextBlock Text=" (" Foreground="Gray"/>
                        <TextBlock Text="{Binding Tables.Count}" Foreground="Gray"/>
                        <TextBlock Text=" tables)" Foreground="Gray"/>
                    </StackPanel>
                </HierarchicalDataTemplate>

                <!-- Template hiển thị Table -->
                <HierarchicalDataTemplate DataType="{x:Type dbmodels:Table}">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="📄" FontSize="14" Margin="0,0,6,0"/>
                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold" />
                    </StackPanel>
                </HierarchicalDataTemplate>

            </TreeView.Resources>
        </TreeView>

        <GridSplitter
            Grid.Column="1"
            Width="5"
            Background="{ui:ThemeResource TextFillColorDisabledBrush}"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            ResizeBehavior="PreviousAndNext"
            ResizeDirection="Columns"/>

        <Border Grid.Column="2">
            <TextBlock Text="Panel phải" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        </Border>
    </Grid>
</UserControl>
